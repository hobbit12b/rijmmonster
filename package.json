{
  "name": "rijmmonster",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-static.mjs && tsc -p tsconfig.json",
    "dev": "npm run build && python3 -m http.server 5173 -d dist",
    "preview": "python3 -m http.server 4173 -d dist"
  },
  "devDependencies": {
    "typescript": "latest"
  }
}
