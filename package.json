{
  "name": "rijmmonster",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/index.html && cp -R public/assets dist/assets && touch dist/.nojekyll && find src -name '*.css' -exec cp --parents {} dist \\; && tsc -p tsconfig.json",
    "dev": "npm run build && python3 -m http.server 5173 -d dist",
    "preview": "python3 -m http.server 4173 -d dist"
  },
  "devDependencies": {
    "typescript": "latest"
  }
}
